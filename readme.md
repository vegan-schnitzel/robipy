# `robipy` -- my personal python package!

### Installation

For now, I rename the parent folder to `something.d` and symlink
the `robipy` package somewhere onto the python import path, hm.

***

![meme](etiquette.jpg)
